import React from 'react'

export const UseTitle = ({heading,description}) => {
  return (
    <div className="text-center pt-8">
    <h1>{heading}</h1>
    <h3>{description}</h3>
    
    </div>
  )
}
